# Default profile
spring.profiles.active=dev

# --- H2 Database Settings (for development profile) ---
spring.config.activate.on-profile=dev
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- MySQL Settings (for production profile) ---
# spring.config.activate.on-profile=prod
# spring.datasource.url=jdbc:mysql://localhost:3306/stockdb?useSSL=false&serverTimezone=UTC
# spring.datasource.username=your-mysql-user
# spring.datasource.password=your-mysql-password
# spring.jpa.hibernate.ddl-auto=validate